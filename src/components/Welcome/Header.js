import React from 'react';
import { HeaderContainer, TextLogo, NavigationButton } from '../../utils/styles';

const Header = () => {
  return <HeaderContainer>
    <TextLogo>healAssis.</TextLogo>
    <NavigationButton />
  </HeaderContainer>;
};

export default Header;
